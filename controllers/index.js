const Identity = require('./identity');
const Admin = require('./admin');
const Manager = require('./manager');
const Executant = require('./executant');

module.exports = {
  Identity,
  Admin,
  Manager,
  Executant,
};
